<UserControl x:Class="EnvironmentAssessment.Controls.ProgressPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DockPanel Margin="15,0,24,12"  >
            <Grid DockPanel.Dock="Top" >
                <Label Name="lblProgress" Content="" VerticalAlignment="Top" Margin="0,0,0,4"/>
            </Grid>
            <Grid HorizontalAlignment="Stretch" DockPanel.Dock="Top" >
                <ProgressBar Name="pbProgress" Height="25" Margin="0,0,0,0" HorizontalAlignment="Stretch" VerticalAlignment="Top"/>
            </Grid>
            <Grid>
                <ListView Name="lvProgress" HorizontalAlignment="Stretch" DockPanel.Dock="Bottom" Margin="0,10,0,0">
                </ListView>
            </Grid>
        </DockPanel>
    </Grid>
</UserControl>

