namespace EnvironmentAssessment.Common.VimApi
{
	public class MessageBusProxy : ViewBase
	{
		public MessageBusProxy(VimClient client, ManagedObjectReference moRef) : base(client, moRef)
		{
		}
	}
}
